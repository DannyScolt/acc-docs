## 1. CSS

- [x] 1.1 Thêm CSS rule `.kv-table` vào phần `<style>` trong `<head>`: `table-layout: fixed` + `td:first-child / th:first-child { width: 22% }`

## 2. HTML

- [x] 2.1 Dùng `replace_all` thêm `class="kv-table"` vào tất cả 55 bảng `<table>` có header `Nội dung | Chi tiết`

## 3. Kiểm tra

- [x] 3.1 Xác nhận cột label thẳng hàng nhau trong preview (Phase 5 và Phase 6)
- [x] 3.2 Xác nhận bảng 3-cột và 5-cột không bị ảnh hưởng
